{
  "name": "dads-daughters-beauty-bond",
  "version": "1.0.0",
  "description": "Beauty Bond — repository root. The deployable service is backend/; app/ is the Expo mobile client and is not deployed here.",
  "private": true,
  "engines": {
    "node": "22.x"
  },
  "scripts": {
    "install:backend": "npm ci --omit=dev --prefix backend",
    "postinstall": "npm run install:backend",
    "start": "node backend/src/server.js"
  }
}
